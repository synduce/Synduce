
let join x (x0, x00) (x1, x10) = (x, ((x0 < x) && (x < x1)) && (x00 && x10))

target⟨join⟩(): int tree -> (int * bool)  = 
{
  ‣ main t1   ⟹  h t1
    h  Leaf(a)  ⟹  (a, true)
    h  Node(a, l, r)  ⟹  join a (h l) (h r)
  
  }
