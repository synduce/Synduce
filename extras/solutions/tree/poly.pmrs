
let join x7 x8 (x9, x90) (x10, x100) =
  (x9 + (x90 × (x8 + (x7 × x10))), x7 × (x90 × x100))

let s0  = (0, 1)

target⟨join, s0⟩(poly_in): int tree -> (int * int)  = 
{
  ‣ main t1   ⟹  h t1
    h  Nil  ⟹  s0
    h  Node(a, l, r)  ⟹  join poly_in a (h l) (h r)
  
  }
