
let odot (x3, x30) (x4, x40) = (min x3 x4, min x30 (max x4 (min x3 x40)))

let f_0 x x0 = (min x x0, max x x0)

target⟨odot, f_0⟩(l): int clist -> (int * int)  = 
{
  ‣ target t   ⟹  h t
    h  CTwo(a, b)  ⟹  f_0 a b
    h  Concat(x, y)  ⟹  odot (h x) (h y)
  
  }
