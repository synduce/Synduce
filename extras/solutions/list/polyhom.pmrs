
let odot x (x0, x00) (x1, x10) = (x1 + (x0 × x10), x00 × x10)

let s0  = (0, 1)

target⟨odot, s0⟩(x): int clist -> (int * int)  = 
{
  ‣ main t   ⟹  h t
    h  CNil  ⟹  s0
    h  Single(a)  ⟹  (a, x)
    h  Concat(y, z)  ⟹  odot x (h y) (h z)
  
  }
