
let s_0  = 0

let odot (x1, x10) (x2, x20) =
  (min x1 x2, x1 = x2 ? x10 + x20 : x2 > x1 ? x10 : x20)

let f_0 x = (x, 1)

target⟨odot, f_0, s_0⟩(l): int clist -> (int * int)  = 
{
  ‣ h  Single(a)  ⟹  f_0 a
  ‣ h  Concat(x, y)  ⟹  odot (h x) (h y)
  
  }
