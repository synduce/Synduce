
let odot (x1, x10) (x2, x20) = (max x1 (x10 + x2), x10 + x20)

let s0  = (0, 0)

let f0 x = (max x 0, x)

target⟨odot, s0, f0⟩(): int clist -> (int * int)  = 
{
  ‣ h  CNil  ⟹  s0
  ‣ h  Single(a)  ⟹  f0 a
  ‣ h  Concat(y, z)  ⟹  odot (h y) (h z)
  
  }
