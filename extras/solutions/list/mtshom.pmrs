
let join (x1, x10) (x2, x20) = (max x2 (x1 + x20), x10 + x20)

let s0  = (0, 0)

let f0 x = (max 0 x, x)

target⟨join, s0, f0⟩(): int clist -> (int * int)  = 
{
  ‣ main t   ⟹  h t
    h  CNil  ⟹  s0
    h  Single(a)  ⟹  f0 a
    h  Concat(y, z)  ⟹  join (h y) (h z)
  
  }
