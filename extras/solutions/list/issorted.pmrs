
let odot (x1, x10, x11) (x2, x20, x21) =
  (x1, x20, x11 && (x21 && (x2 > x10)))

target⟨odot⟩(): int clist -> (int * int * bool)  = 
{
  ‣ main t   ⟹  h t
    h  Single(a)  ⟹  (a, a, true)
    h  Concat(y, z)  ⟹  odot (h y) (h z)
  
  }
