type 'a clist = CNil | Single of 'a | Concat of 'a list * 'a list

type 'a list = Nil | Cons of 'a * 'a list


pmrs (odot,f_0,s_0) listhom t =
    main t           -> h t
    | h CNil          -> s_0
    | h Single(a)    -> f_0 a
    | h Concat(x, y) -> odot (h x) (h y)
